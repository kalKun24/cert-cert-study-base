# TICKET-051 TeamDetailPageからメンバー一覧への導線追加

## 基本情報

| 項目 | 内容 |
|---|---|
| チケットID | TICKET-051 |
| ステータス | 🟡 作業中 |
| 作成日 | 2026-06-20 |
| 着手日 | 2026-06-20 |
| 完了日 | - |
| ブランチ名 | `feature/team-member-list-link` |
| PR番号 | - |
| PRリンク | - |

---

## 概要

チームメンバー一覧ページ（`/teams/{id}/members`）が TICKET-042 で実装済みだが、`TeamDetailPage` 等からの UI 導線がない。メンバー数表示をリンク化し、一覧ページへ遷移できるようにする。

---

## 背景・目的

TICKET-042 実装時に「TeamDetailPage からメンバー一覧への導線」がスコープ外となった。ホーム画面の「チームメンバー数」クリック時の遷移も含め、ユーザーが一覧ページへ自然に辿り着けるようにする。

---

## 受け入れ条件

- [ ] `TeamDetailPage` にメンバー一覧（`/teams/{id}/members`）へのリンクが存在する
- [ ] `HomePage` の「チームメンバー数」表示がリンクになっており、クリックで `/teams/{id}/members` へ遷移する
- [ ] 追加した文字列は `locales/ja.json` で管理されている

---

## サブチケット（コミット単位）

- [ ] `feat(page): TeamDetailPage にメンバー一覧へのリンクを追加`
- [ ] `feat(page): HomePage のチームメンバー数をリンク化`

---

## 関連情報

- 関連チケット: TICKET-042（チームメンバー一覧画面）、TICKET-041（ダッシュボード）
- 備考: TICKET-042 の PR #32 で「次スプリント検討事項」として積み残した導線追加タスク
