# TICKET-005 タグ管理API実装

## 基本情報

| 項目 | 内容 |
|---|---|
| チケットID | TICKET-005 |
| ステータス | 🟡 作業中 |
| 作成日 | 2026-06-17 |
| 着手日 | 2026-06-18 |
| 完了日 | - |
| ブランチ名 | `feature/tag-api` |
| PR番号 | - |
| PRリンク | （PR作成後に記入） |

---

## 概要

問題に付与するタグのCRUD APIを実装する。タグはフラット構造で複数付与可能とし、問題の分類・検索・フィルタリングの基盤となる。

---

## 背景・目的

CISSP・情報処理安全確保支援士の問題を「暗号化」「アクセス制御」「リスク管理」などのカテゴリで分類できるようにするため、柔軟なタグ機能が必要。タグはフラット構造（階層なし）で複数付与可能とし、管理のシンプルさを維持する。

---

## 受け入れ条件

- [ ] `GET /api/v1/tags` でタグ一覧を取得できる
- [ ] `POST /api/v1/tags` で新しいタグを作成できる（`admin`のみ）
- [ ] `PUT /api/v1/tags/{id}` でタグ名を更新できる（`admin`のみ）
- [ ] `DELETE /api/v1/tags/{id}` でタグを削除できる（`admin`のみ、使用中タグは削除不可 or 問題から自動除去を明示）
- [ ] タグエンティティは `id` / `name` / `created_at` を持つ
- [ ] タグ名の重複登録はエラー（409 Conflict）を返す
- [ ] `openapi.yaml` にタグ管理エンドポイントのSwagger定義が存在する
- [ ] ユースケース層のユニットテストが作成されている

---

## サブチケット（コミット単位）

- [ ] `docs(api): タグ管理エンドポイントをopenapi.yamlに追加`
- [ ] `feat(domain): Tagエンティティを実装`
- [ ] `feat(usecase): タグCRUDユースケースを実装`
- [ ] `feat(interface): タグCRUDハンドラとDTOを実装`
- [ ] `feat(infrastructure): タグのGCSリポジトリ実装`
- [ ] `test(usecase): タグCRUDユースケースのユニットテストを作成`

---

## 関連情報

- 関連チケット: TICKET-001（前提）、TICKET-002（認証ミドルウェア適用のため前提）、TICKET-004（QuestionエンティティのtagsフィールドはTag IDの配列で参照）、TICKET-008（タグ検索・フィルタリング）
- 参考: CLAUDE.md「主な機能」セクション（タグはフラット・複数付与可）
- 備考: タグ削除時の問題への影響（使用中タグの扱い）は設計判断が必要。着手前に方針を確認する
